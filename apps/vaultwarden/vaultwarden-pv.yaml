apiVersion: v1
kind: PersistentVolume
metadata:
  name: vaultwarden
  labels:
    type: local
spec:
  capacity:
    storage: 50Gi
  accessModes:
  - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /data/vaultwarden
