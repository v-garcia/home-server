apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: headlamp-ext
  labels:
    type: external
    authentified: "true"
spec:
  tls:
    - hosts:
        - headlamp.{hostname}
      secretName: headlamp.ing.cert
  rules:
    - host: headlamp.{hostname}
      http:
        paths:
          - backend:
              service:
                name: headlamp
                port:
                  number: 80
            path: /
            pathType: Prefix
