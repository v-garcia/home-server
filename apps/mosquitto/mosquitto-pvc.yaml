apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mosquitto
spec:
  storageClassName: ''
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  volumeName: mosquitto
