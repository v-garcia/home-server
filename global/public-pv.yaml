apiVersion: v1
kind: PersistentVolume
metadata:
  name: public
  labels:
    type: local
spec:
  capacity:
    storage: 500Gi
  accessModes:
  - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /data/public
